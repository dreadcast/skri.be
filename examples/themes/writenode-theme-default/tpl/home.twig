{% block meta %}
	<title>{{ site.title }}</title>
	<meta name="description" content="{{ site.description }}">
	<meta name="keywords" content="{{ site.featuredTags|join(', ') }}">
{% endblock %}

{% extends 'default.twig' %}